<% @accounts.each do |account| %>
poll <%= account['pop_server'] %> protocol pop3 username "<%= account['email'] %>" password "<%= account['password'] %>" is "<%= account['local_email'] %>" ssl keep
<% end %>
