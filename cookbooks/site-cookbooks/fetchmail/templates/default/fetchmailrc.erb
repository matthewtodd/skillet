<% @accounts.each do |account| %>
poll <%= account['pop_server'] %> protocol pop3 ssl keep username "<%= account['email'] %>" password "<%= account['password'] %>" is "<%= account['local_email'] %>"
<% end %>
