###
# Optional SMTP client lookup tables with one username:password entry per
# remote hostname or domain, or sender address when sender-dependent
# authentication is enabled. If no username:password entry is found, then the
# Postfix SMTP client will not attempt to authenticate to the remote host.
#
# Generated by Chef for <%= @node[:fqdn] %>
###

user     = root
password = <%= @node[:mysql][:server_root_password] %>
dbname   = <%= @node[:hectic][:db][:database] %>
query    = SELECT smtp_credentials FROM hosts WHERE smtp_server='%s'
