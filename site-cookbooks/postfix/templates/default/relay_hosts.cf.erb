###
# A sender-dependent override for the global relayhost parameter setting.
# The tables are searched by the envelope sender address and @domain.
#
# Generated by Chef for <%= @node[:fqdn] %>
###

user     = root
password = <%= @node[:mysql][:server_root_password] %>
dbname   = <%= @node[:hectic][:db][:database] %>
query    = SELECT hosts.smtp_server FROM hosts JOIN accounts ON hosts.id = accounts.host_id WHERE accounts.public_email='%s'
